<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Uploaded PDFs</title>
</head>
<body>
<h1>Uploaded PDFs</h1>
<ul>
  <% files.forEach(file => { %>
    <li>
      <%= file %>
      - <a href="/user/<%= username %>/pdfs/<%= file %>" target="_blank">View</a>
      - <a href="/user/<%= username %>/rename/<%= file %>">Rename</a>
      - <a href="/user/<%= username %>/download/<%= file %>">Download</a>
      - <a href="/user/<%= username %>/delete/<%= file %>" onclick="return confirm('Are you sure?');">Delete</a>
    </li>
  <% }); %>
</ul>
<a href="/">Upload More PDFs</a>
</body>
</html>
